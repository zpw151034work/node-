#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../_acorn@1.2.2@acorn/bin/acorn" "$@"
else 
  exec node  "$basedir/../_acorn@1.2.2@acorn/bin/acorn" "$@"
fi
